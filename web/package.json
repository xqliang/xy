{
  "name": "dasheng-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "《大圣与唐僧》Web 可玩原型（复用 game-core 数值内核）",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5180",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "puppeteer-core": "^23.11.1",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
