addr: "0.0.0.0:8082"
static_dir: "/opt/xy/html"
timezone: "Asia/Shanghai"
db:
  host: "127.0.0.1"
  port: 3306
  user: "xy_game"
  password: "CHANGE_ME"
  database: "xy_game"
redis:
  host: "127.0.0.1"   # PvP 匹配/对局状态存储；与其它项目共用同一 Redis，key 统一带 xy:pvp: 前缀
  port: 6379
  db: 0
  password: ""        # 空串=无密码（本地默认）；线上按需填写
admin:
  username: "admin"
  password: "CHANGE_ME"
wechat:
  appid: ""      # 小游戏 AppID
  secret: ""     # AppSecret —— 仅服务端持有，绝不进客户端包
auth:
  strict: false  # 灰度：false=无 token 回退认 X-Uid；前端全量带 token 后置 true
  session_days: 30
# optional TOS backup (leave empty to skip upload)
tos:
  endpoint: ""
  region: ""
  bucket: ""
  access_key: ""
  secret_key: ""